version: '3'

services:
  kibana:    
    environment:
      ELASTICSEARCH_URL: http://es.measures.globoi.com:80   
    image: docker.elastic.co/kibana/kibana-oss:6.2.1
    networks:
      - app
    ports:
    - 5601:5601
    restart: unless-stopped

networks: 
  app:
    driver: bridge

